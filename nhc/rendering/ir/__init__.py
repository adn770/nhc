"""Floor IR — FlatBuffers-backed rendering intermediate representation.

The schema is in ``floor_ir.fbs``; FB-generated Python bindings live
in the ``_fb`` sub-package and are regenerated by ``make
ir-bindings``. Hand-written helpers (e.g. ``dump.py``) sit at this
package level so the generated tree stays untouched between
regenerations.

Every type listed in ``floor_ir.fbs`` is reachable via
``nhc.rendering.ir._fb.<TableName>``. Importers should pull from
the ``_fb`` sub-package directly for now; once the IR migration's
Phase 1 emitter lands, the most-used types will be re-exported
here for ergonomics.
"""
