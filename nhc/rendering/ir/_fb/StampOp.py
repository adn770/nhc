# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class StampOp(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = StampOp()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsStampOp(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def StampOpBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x35", size_prefixed=size_prefixed)

    # StampOp
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # StampOp
    def RegionRef(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # StampOp
    def SubtractRegionRefs(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # StampOp
    def SubtractRegionRefsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # StampOp
    def SubtractRegionRefsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # StampOp
    def DecoratorMask(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # StampOp
    def Density(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 128

    # StampOp
    def Seed(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

def StampOpStart(builder):
    builder.StartObject(5)

def Start(builder):
    StampOpStart(builder)

def StampOpAddRegionRef(builder, regionRef):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(regionRef), 0)

def AddRegionRef(builder, regionRef):
    StampOpAddRegionRef(builder, regionRef)

def StampOpAddSubtractRegionRefs(builder, subtractRegionRefs):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(subtractRegionRefs), 0)

def AddSubtractRegionRefs(builder, subtractRegionRefs):
    StampOpAddSubtractRegionRefs(builder, subtractRegionRefs)

def StampOpStartSubtractRegionRefsVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSubtractRegionRefsVector(builder, numElems):
    return StampOpStartSubtractRegionRefsVector(builder, numElems)

def StampOpAddDecoratorMask(builder, decoratorMask):
    builder.PrependUint32Slot(2, decoratorMask, 0)

def AddDecoratorMask(builder, decoratorMask):
    StampOpAddDecoratorMask(builder, decoratorMask)

def StampOpAddDensity(builder, density):
    builder.PrependUint8Slot(3, density, 128)

def AddDensity(builder, density):
    StampOpAddDensity(builder, density)

def StampOpAddSeed(builder, seed):
    builder.PrependUint64Slot(4, seed, 0)

def AddSeed(builder, seed):
    StampOpAddSeed(builder, seed)

def StampOpEnd(builder):
    return builder.EndObject()

def End(builder):
    return StampOpEnd(builder)

try:
    from typing import List
except:
    pass

class StampOpT(object):

    # StampOpT
    def __init__(
        self,
        regionRef = None,
        subtractRegionRefs = None,
        decoratorMask = 0,
        density = 128,
        seed = 0,
    ):
        self.regionRef = regionRef  # type: Optional[str]
        self.subtractRegionRefs = subtractRegionRefs  # type: Optional[List[Optional[str]]]
        self.decoratorMask = decoratorMask  # type: int
        self.density = density  # type: int
        self.seed = seed  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        stampOp = StampOp()
        stampOp.Init(buf, pos)
        return cls.InitFromObj(stampOp)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, stampOp):
        x = StampOpT()
        x._UnPack(stampOp)
        return x

    # StampOpT
    def _UnPack(self, stampOp):
        if stampOp is None:
            return
        self.regionRef = stampOp.RegionRef()
        if not stampOp.SubtractRegionRefsIsNone():
            self.subtractRegionRefs = []
            for i in range(stampOp.SubtractRegionRefsLength()):
                self.subtractRegionRefs.append(stampOp.SubtractRegionRefs(i))
        self.decoratorMask = stampOp.DecoratorMask()
        self.density = stampOp.Density()
        self.seed = stampOp.Seed()

    # StampOpT
    def Pack(self, builder):
        if self.regionRef is not None:
            regionRef = builder.CreateString(self.regionRef)
        if self.subtractRegionRefs is not None:
            subtractRegionRefslist = []
            for i in range(len(self.subtractRegionRefs)):
                subtractRegionRefslist.append(builder.CreateString(self.subtractRegionRefs[i]))
            StampOpStartSubtractRegionRefsVector(builder, len(self.subtractRegionRefs))
            for i in reversed(range(len(self.subtractRegionRefs))):
                builder.PrependUOffsetTRelative(subtractRegionRefslist[i])
            subtractRegionRefs = builder.EndVector()
        StampOpStart(builder)
        if self.regionRef is not None:
            StampOpAddRegionRef(builder, regionRef)
        if self.subtractRegionRefs is not None:
            StampOpAddSubtractRegionRefs(builder, subtractRegionRefs)
        StampOpAddDecoratorMask(builder, self.decoratorMask)
        StampOpAddDensity(builder, self.density)
        StampOpAddSeed(builder, self.seed)
        stampOp = StampOpEnd(builder)
        return stampOp
