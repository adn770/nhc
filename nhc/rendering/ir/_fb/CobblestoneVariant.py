# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class CobblestoneVariant(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CobblestoneVariant()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCobblestoneVariant(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CobblestoneVariantBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x33", size_prefixed=size_prefixed)

    # CobblestoneVariant
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # CobblestoneVariant
    def Tiles(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 8
            from nhc.rendering.ir._fb.TileCoord import TileCoord
            obj = TileCoord()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # CobblestoneVariant
    def TilesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CobblestoneVariant
    def TilesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # CobblestoneVariant
    def Pattern(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

def CobblestoneVariantStart(builder):
    builder.StartObject(2)

def Start(builder):
    CobblestoneVariantStart(builder)

def CobblestoneVariantAddTiles(builder, tiles):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(tiles), 0)

def AddTiles(builder, tiles):
    CobblestoneVariantAddTiles(builder, tiles)

def CobblestoneVariantStartTilesVector(builder, numElems):
    return builder.StartVector(8, numElems, 4)

def StartTilesVector(builder, numElems):
    return CobblestoneVariantStartTilesVector(builder, numElems)

def CobblestoneVariantAddPattern(builder, pattern):
    builder.PrependInt8Slot(1, pattern, 0)

def AddPattern(builder, pattern):
    CobblestoneVariantAddPattern(builder, pattern)

def CobblestoneVariantEnd(builder):
    return builder.EndObject()

def End(builder):
    return CobblestoneVariantEnd(builder)

import nhc.rendering.ir._fb.TileCoord
try:
    from typing import List
except:
    pass

class CobblestoneVariantT(object):

    # CobblestoneVariantT
    def __init__(
        self,
        tiles = None,
        pattern = 0,
    ):
        self.tiles = tiles  # type: Optional[List[nhc.rendering.ir._fb.TileCoord.TileCoordT]]
        self.pattern = pattern  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        cobblestoneVariant = CobblestoneVariant()
        cobblestoneVariant.Init(buf, pos)
        return cls.InitFromObj(cobblestoneVariant)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, cobblestoneVariant):
        x = CobblestoneVariantT()
        x._UnPack(cobblestoneVariant)
        return x

    # CobblestoneVariantT
    def _UnPack(self, cobblestoneVariant):
        if cobblestoneVariant is None:
            return
        if not cobblestoneVariant.TilesIsNone():
            self.tiles = []
            for i in range(cobblestoneVariant.TilesLength()):
                if cobblestoneVariant.Tiles(i) is None:
                    self.tiles.append(None)
                else:
                    tileCoord_ = nhc.rendering.ir._fb.TileCoord.TileCoordT.InitFromObj(cobblestoneVariant.Tiles(i))
                    self.tiles.append(tileCoord_)
        self.pattern = cobblestoneVariant.Pattern()

    # CobblestoneVariantT
    def Pack(self, builder):
        if self.tiles is not None:
            CobblestoneVariantStartTilesVector(builder, len(self.tiles))
            for i in reversed(range(len(self.tiles))):
                self.tiles[i].Pack(builder)
            tiles = builder.EndVector()
        CobblestoneVariantStart(builder)
        if self.tiles is not None:
            CobblestoneVariantAddTiles(builder, tiles)
        CobblestoneVariantAddPattern(builder, self.pattern)
        cobblestoneVariant = CobblestoneVariantEnd(builder)
        return cobblestoneVariant
