# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class CartTracksOp(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CartTracksOp()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCartTracksOp(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CartTracksOpBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x46", size_prefixed=size_prefixed)

    # CartTracksOp
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # CartTracksOp
    def Tiles(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 8
            from nhc.rendering.ir._fb.TileCoord import TileCoord
            obj = TileCoord()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # CartTracksOp
    def TilesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CartTracksOp
    def TilesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # CartTracksOp
    def Seed(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

def CartTracksOpStart(builder):
    builder.StartObject(2)

def Start(builder):
    CartTracksOpStart(builder)

def CartTracksOpAddTiles(builder, tiles):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(tiles), 0)

def AddTiles(builder, tiles):
    CartTracksOpAddTiles(builder, tiles)

def CartTracksOpStartTilesVector(builder, numElems):
    return builder.StartVector(8, numElems, 4)

def StartTilesVector(builder, numElems):
    return CartTracksOpStartTilesVector(builder, numElems)

def CartTracksOpAddSeed(builder, seed):
    builder.PrependUint64Slot(1, seed, 0)

def AddSeed(builder, seed):
    CartTracksOpAddSeed(builder, seed)

def CartTracksOpEnd(builder):
    return builder.EndObject()

def End(builder):
    return CartTracksOpEnd(builder)

import nhc.rendering.ir._fb.TileCoord
try:
    from typing import List
except:
    pass

class CartTracksOpT(object):

    # CartTracksOpT
    def __init__(
        self,
        tiles = None,
        seed = 0,
    ):
        self.tiles = tiles  # type: Optional[List[nhc.rendering.ir._fb.TileCoord.TileCoordT]]
        self.seed = seed  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        cartTracksOp = CartTracksOp()
        cartTracksOp.Init(buf, pos)
        return cls.InitFromObj(cartTracksOp)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, cartTracksOp):
        x = CartTracksOpT()
        x._UnPack(cartTracksOp)
        return x

    # CartTracksOpT
    def _UnPack(self, cartTracksOp):
        if cartTracksOp is None:
            return
        if not cartTracksOp.TilesIsNone():
            self.tiles = []
            for i in range(cartTracksOp.TilesLength()):
                if cartTracksOp.Tiles(i) is None:
                    self.tiles.append(None)
                else:
                    tileCoord_ = nhc.rendering.ir._fb.TileCoord.TileCoordT.InitFromObj(cartTracksOp.Tiles(i))
                    self.tiles.append(tileCoord_)
        self.seed = cartTracksOp.Seed()

    # CartTracksOpT
    def Pack(self, builder):
        if self.tiles is not None:
            CartTracksOpStartTilesVector(builder, len(self.tiles))
            for i in reversed(range(len(self.tiles))):
                self.tiles[i].Pack(builder)
            tiles = builder.EndVector()
        CartTracksOpStart(builder)
        if self.tiles is not None:
            CartTracksOpAddTiles(builder, tiles)
        CartTracksOpAddSeed(builder, self.seed)
        cartTracksOp = CartTracksOpEnd(builder)
        return cartTracksOp
