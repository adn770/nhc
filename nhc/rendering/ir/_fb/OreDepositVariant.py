# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class OreDepositVariant(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = OreDepositVariant()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsOreDepositVariant(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def OreDepositVariantBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x33", size_prefixed=size_prefixed)

    # OreDepositVariant
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # OreDepositVariant
    def Tiles(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 8
            from nhc.rendering.ir._fb.TileCoord import TileCoord
            obj = TileCoord()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # OreDepositVariant
    def TilesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # OreDepositVariant
    def TilesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def OreDepositVariantStart(builder):
    builder.StartObject(1)

def Start(builder):
    OreDepositVariantStart(builder)

def OreDepositVariantAddTiles(builder, tiles):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(tiles), 0)

def AddTiles(builder, tiles):
    OreDepositVariantAddTiles(builder, tiles)

def OreDepositVariantStartTilesVector(builder, numElems):
    return builder.StartVector(8, numElems, 4)

def StartTilesVector(builder, numElems):
    return OreDepositVariantStartTilesVector(builder, numElems)

def OreDepositVariantEnd(builder):
    return builder.EndObject()

def End(builder):
    return OreDepositVariantEnd(builder)

import nhc.rendering.ir._fb.TileCoord
try:
    from typing import List
except:
    pass

class OreDepositVariantT(object):

    # OreDepositVariantT
    def __init__(
        self,
        tiles = None,
    ):
        self.tiles = tiles  # type: Optional[List[nhc.rendering.ir._fb.TileCoord.TileCoordT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        oreDepositVariant = OreDepositVariant()
        oreDepositVariant.Init(buf, pos)
        return cls.InitFromObj(oreDepositVariant)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, oreDepositVariant):
        x = OreDepositVariantT()
        x._UnPack(oreDepositVariant)
        return x

    # OreDepositVariantT
    def _UnPack(self, oreDepositVariant):
        if oreDepositVariant is None:
            return
        if not oreDepositVariant.TilesIsNone():
            self.tiles = []
            for i in range(oreDepositVariant.TilesLength()):
                if oreDepositVariant.Tiles(i) is None:
                    self.tiles.append(None)
                else:
                    tileCoord_ = nhc.rendering.ir._fb.TileCoord.TileCoordT.InitFromObj(oreDepositVariant.Tiles(i))
                    self.tiles.append(tileCoord_)

    # OreDepositVariantT
    def Pack(self, builder):
        if self.tiles is not None:
            OreDepositVariantStartTilesVector(builder, len(self.tiles))
            for i in reversed(range(len(self.tiles))):
                self.tiles[i].Pack(builder)
            tiles = builder.EndVector()
        OreDepositVariantStart(builder)
        if self.tiles is not None:
            OreDepositVariantAddTiles(builder, tiles)
        oreDepositVariant = OreDepositVariantEnd(builder)
        return oreDepositVariant
