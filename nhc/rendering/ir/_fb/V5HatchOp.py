# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class V5HatchOp(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = V5HatchOp()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsV5HatchOp(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def V5HatchOpBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x34", size_prefixed=size_prefixed)

    # V5HatchOp
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # V5HatchOp
    def Kind(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # V5HatchOp
    def RegionRef(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # V5HatchOp
    def SubtractRegionRefs(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # V5HatchOp
    def SubtractRegionRefsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # V5HatchOp
    def SubtractRegionRefsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # V5HatchOp
    def Tiles(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 8
            from nhc.rendering.ir._fb.TileCoord import TileCoord
            obj = TileCoord()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # V5HatchOp
    def TilesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # V5HatchOp
    def TilesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

    # V5HatchOp
    def IsOuter(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.BoolFlags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # V5HatchOp
    def IsOuterAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.BoolFlags, o)
        return 0

    # V5HatchOp
    def IsOuterLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # V5HatchOp
    def IsOuterIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # V5HatchOp
    def ExtentTiles(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 2.0

    # V5HatchOp
    def Seed(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # V5HatchOp
    def HatchUnderlayColor(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def V5HatchOpStart(builder):
    builder.StartObject(8)

def Start(builder):
    V5HatchOpStart(builder)

def V5HatchOpAddKind(builder, kind):
    builder.PrependInt8Slot(0, kind, 0)

def AddKind(builder, kind):
    V5HatchOpAddKind(builder, kind)

def V5HatchOpAddRegionRef(builder, regionRef):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(regionRef), 0)

def AddRegionRef(builder, regionRef):
    V5HatchOpAddRegionRef(builder, regionRef)

def V5HatchOpAddSubtractRegionRefs(builder, subtractRegionRefs):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(subtractRegionRefs), 0)

def AddSubtractRegionRefs(builder, subtractRegionRefs):
    V5HatchOpAddSubtractRegionRefs(builder, subtractRegionRefs)

def V5HatchOpStartSubtractRegionRefsVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSubtractRegionRefsVector(builder, numElems):
    return V5HatchOpStartSubtractRegionRefsVector(builder, numElems)

def V5HatchOpAddTiles(builder, tiles):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(tiles), 0)

def AddTiles(builder, tiles):
    V5HatchOpAddTiles(builder, tiles)

def V5HatchOpStartTilesVector(builder, numElems):
    return builder.StartVector(8, numElems, 4)

def StartTilesVector(builder, numElems):
    return V5HatchOpStartTilesVector(builder, numElems)

def V5HatchOpAddIsOuter(builder, isOuter):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(isOuter), 0)

def AddIsOuter(builder, isOuter):
    V5HatchOpAddIsOuter(builder, isOuter)

def V5HatchOpStartIsOuterVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartIsOuterVector(builder, numElems):
    return V5HatchOpStartIsOuterVector(builder, numElems)

def V5HatchOpAddExtentTiles(builder, extentTiles):
    builder.PrependFloat32Slot(5, extentTiles, 2.0)

def AddExtentTiles(builder, extentTiles):
    V5HatchOpAddExtentTiles(builder, extentTiles)

def V5HatchOpAddSeed(builder, seed):
    builder.PrependUint64Slot(6, seed, 0)

def AddSeed(builder, seed):
    V5HatchOpAddSeed(builder, seed)

def V5HatchOpAddHatchUnderlayColor(builder, hatchUnderlayColor):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(hatchUnderlayColor), 0)

def AddHatchUnderlayColor(builder, hatchUnderlayColor):
    V5HatchOpAddHatchUnderlayColor(builder, hatchUnderlayColor)

def V5HatchOpEnd(builder):
    return builder.EndObject()

def End(builder):
    return V5HatchOpEnd(builder)

import nhc.rendering.ir._fb.TileCoord
try:
    from typing import List
except:
    pass

class V5HatchOpT(object):

    # V5HatchOpT
    def __init__(
        self,
        kind = 0,
        regionRef = None,
        subtractRegionRefs = None,
        tiles = None,
        isOuter = None,
        extentTiles = 2.0,
        seed = 0,
        hatchUnderlayColor = None,
    ):
        self.kind = kind  # type: int
        self.regionRef = regionRef  # type: Optional[str]
        self.subtractRegionRefs = subtractRegionRefs  # type: Optional[List[Optional[str]]]
        self.tiles = tiles  # type: Optional[List[nhc.rendering.ir._fb.TileCoord.TileCoordT]]
        self.isOuter = isOuter  # type: Optional[List[bool]]
        self.extentTiles = extentTiles  # type: float
        self.seed = seed  # type: int
        self.hatchUnderlayColor = hatchUnderlayColor  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        v5HatchOp = V5HatchOp()
        v5HatchOp.Init(buf, pos)
        return cls.InitFromObj(v5HatchOp)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, v5HatchOp):
        x = V5HatchOpT()
        x._UnPack(v5HatchOp)
        return x

    # V5HatchOpT
    def _UnPack(self, v5HatchOp):
        if v5HatchOp is None:
            return
        self.kind = v5HatchOp.Kind()
        self.regionRef = v5HatchOp.RegionRef()
        if not v5HatchOp.SubtractRegionRefsIsNone():
            self.subtractRegionRefs = []
            for i in range(v5HatchOp.SubtractRegionRefsLength()):
                self.subtractRegionRefs.append(v5HatchOp.SubtractRegionRefs(i))
        if not v5HatchOp.TilesIsNone():
            self.tiles = []
            for i in range(v5HatchOp.TilesLength()):
                if v5HatchOp.Tiles(i) is None:
                    self.tiles.append(None)
                else:
                    tileCoord_ = nhc.rendering.ir._fb.TileCoord.TileCoordT.InitFromObj(v5HatchOp.Tiles(i))
                    self.tiles.append(tileCoord_)
        if not v5HatchOp.IsOuterIsNone():
            if np is None:
                self.isOuter = []
                for i in range(v5HatchOp.IsOuterLength()):
                    self.isOuter.append(v5HatchOp.IsOuter(i))
            else:
                self.isOuter = v5HatchOp.IsOuterAsNumpy()
        self.extentTiles = v5HatchOp.ExtentTiles()
        self.seed = v5HatchOp.Seed()
        self.hatchUnderlayColor = v5HatchOp.HatchUnderlayColor()

    # V5HatchOpT
    def Pack(self, builder):
        if self.regionRef is not None:
            regionRef = builder.CreateString(self.regionRef)
        if self.subtractRegionRefs is not None:
            subtractRegionRefslist = []
            for i in range(len(self.subtractRegionRefs)):
                subtractRegionRefslist.append(builder.CreateString(self.subtractRegionRefs[i]))
            V5HatchOpStartSubtractRegionRefsVector(builder, len(self.subtractRegionRefs))
            for i in reversed(range(len(self.subtractRegionRefs))):
                builder.PrependUOffsetTRelative(subtractRegionRefslist[i])
            subtractRegionRefs = builder.EndVector()
        if self.tiles is not None:
            V5HatchOpStartTilesVector(builder, len(self.tiles))
            for i in reversed(range(len(self.tiles))):
                self.tiles[i].Pack(builder)
            tiles = builder.EndVector()
        if self.isOuter is not None:
            if np is not None and type(self.isOuter) is np.ndarray:
                isOuter = builder.CreateNumpyVector(self.isOuter)
            else:
                V5HatchOpStartIsOuterVector(builder, len(self.isOuter))
                for i in reversed(range(len(self.isOuter))):
                    builder.PrependBool(self.isOuter[i])
                isOuter = builder.EndVector()
        if self.hatchUnderlayColor is not None:
            hatchUnderlayColor = builder.CreateString(self.hatchUnderlayColor)
        V5HatchOpStart(builder)
        V5HatchOpAddKind(builder, self.kind)
        if self.regionRef is not None:
            V5HatchOpAddRegionRef(builder, regionRef)
        if self.subtractRegionRefs is not None:
            V5HatchOpAddSubtractRegionRefs(builder, subtractRegionRefs)
        if self.tiles is not None:
            V5HatchOpAddTiles(builder, tiles)
        if self.isOuter is not None:
            V5HatchOpAddIsOuter(builder, isOuter)
        V5HatchOpAddExtentTiles(builder, self.extentTiles)
        V5HatchOpAddSeed(builder, self.seed)
        if self.hatchUnderlayColor is not None:
            V5HatchOpAddHatchUnderlayColor(builder, hatchUnderlayColor)
        v5HatchOp = V5HatchOpEnd(builder)
        return v5HatchOp
