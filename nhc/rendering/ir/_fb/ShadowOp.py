# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class ShadowOp(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ShadowOp()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsShadowOp(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def ShadowOpBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x34", size_prefixed=size_prefixed)

    # ShadowOp
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ShadowOp
    def Kind(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # ShadowOp
    def RegionRef(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ShadowOp
    def Tiles(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 8
            from nhc.rendering.ir._fb.TileCoord import TileCoord
            obj = TileCoord()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ShadowOp
    def TilesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ShadowOp
    def TilesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # ShadowOp
    def Dx(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 3.0

    # ShadowOp
    def Dy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 3.0

    # ShadowOp
    def Opacity(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.08

def ShadowOpStart(builder):
    builder.StartObject(6)

def Start(builder):
    ShadowOpStart(builder)

def ShadowOpAddKind(builder, kind):
    builder.PrependInt8Slot(0, kind, 0)

def AddKind(builder, kind):
    ShadowOpAddKind(builder, kind)

def ShadowOpAddRegionRef(builder, regionRef):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(regionRef), 0)

def AddRegionRef(builder, regionRef):
    ShadowOpAddRegionRef(builder, regionRef)

def ShadowOpAddTiles(builder, tiles):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(tiles), 0)

def AddTiles(builder, tiles):
    ShadowOpAddTiles(builder, tiles)

def ShadowOpStartTilesVector(builder, numElems):
    return builder.StartVector(8, numElems, 4)

def StartTilesVector(builder, numElems):
    return ShadowOpStartTilesVector(builder, numElems)

def ShadowOpAddDx(builder, dx):
    builder.PrependFloat32Slot(3, dx, 3.0)

def AddDx(builder, dx):
    ShadowOpAddDx(builder, dx)

def ShadowOpAddDy(builder, dy):
    builder.PrependFloat32Slot(4, dy, 3.0)

def AddDy(builder, dy):
    ShadowOpAddDy(builder, dy)

def ShadowOpAddOpacity(builder, opacity):
    builder.PrependFloat32Slot(5, opacity, 0.08)

def AddOpacity(builder, opacity):
    ShadowOpAddOpacity(builder, opacity)

def ShadowOpEnd(builder):
    return builder.EndObject()

def End(builder):
    return ShadowOpEnd(builder)

import nhc.rendering.ir._fb.TileCoord
try:
    from typing import List
except:
    pass

class ShadowOpT(object):

    # ShadowOpT
    def __init__(
        self,
        kind = 0,
        regionRef = None,
        tiles = None,
        dx = 3.0,
        dy = 3.0,
        opacity = 0.08,
    ):
        self.kind = kind  # type: int
        self.regionRef = regionRef  # type: Optional[str]
        self.tiles = tiles  # type: Optional[List[nhc.rendering.ir._fb.TileCoord.TileCoordT]]
        self.dx = dx  # type: float
        self.dy = dy  # type: float
        self.opacity = opacity  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        shadowOp = ShadowOp()
        shadowOp.Init(buf, pos)
        return cls.InitFromObj(shadowOp)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, shadowOp):
        x = ShadowOpT()
        x._UnPack(shadowOp)
        return x

    # ShadowOpT
    def _UnPack(self, shadowOp):
        if shadowOp is None:
            return
        self.kind = shadowOp.Kind()
        self.regionRef = shadowOp.RegionRef()
        if not shadowOp.TilesIsNone():
            self.tiles = []
            for i in range(shadowOp.TilesLength()):
                if shadowOp.Tiles(i) is None:
                    self.tiles.append(None)
                else:
                    tileCoord_ = nhc.rendering.ir._fb.TileCoord.TileCoordT.InitFromObj(shadowOp.Tiles(i))
                    self.tiles.append(tileCoord_)
        self.dx = shadowOp.Dx()
        self.dy = shadowOp.Dy()
        self.opacity = shadowOp.Opacity()

    # ShadowOpT
    def Pack(self, builder):
        if self.regionRef is not None:
            regionRef = builder.CreateString(self.regionRef)
        if self.tiles is not None:
            ShadowOpStartTilesVector(builder, len(self.tiles))
            for i in reversed(range(len(self.tiles))):
                self.tiles[i].Pack(builder)
            tiles = builder.EndVector()
        ShadowOpStart(builder)
        ShadowOpAddKind(builder, self.kind)
        if self.regionRef is not None:
            ShadowOpAddRegionRef(builder, regionRef)
        if self.tiles is not None:
            ShadowOpAddTiles(builder, tiles)
        ShadowOpAddDx(builder, self.dx)
        ShadowOpAddDy(builder, self.dy)
        ShadowOpAddOpacity(builder, self.opacity)
        shadowOp = ShadowOpEnd(builder)
        return shadowOp
