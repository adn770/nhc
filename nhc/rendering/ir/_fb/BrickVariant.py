# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class BrickVariant(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = BrickVariant()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsBrickVariant(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def BrickVariantBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x33", size_prefixed=size_prefixed)

    # BrickVariant
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # BrickVariant
    def Tiles(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 8
            from nhc.rendering.ir._fb.TileCoord import TileCoord
            obj = TileCoord()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # BrickVariant
    def TilesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # BrickVariant
    def TilesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def BrickVariantStart(builder):
    builder.StartObject(1)

def Start(builder):
    BrickVariantStart(builder)

def BrickVariantAddTiles(builder, tiles):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(tiles), 0)

def AddTiles(builder, tiles):
    BrickVariantAddTiles(builder, tiles)

def BrickVariantStartTilesVector(builder, numElems):
    return builder.StartVector(8, numElems, 4)

def StartTilesVector(builder, numElems):
    return BrickVariantStartTilesVector(builder, numElems)

def BrickVariantEnd(builder):
    return builder.EndObject()

def End(builder):
    return BrickVariantEnd(builder)

import nhc.rendering.ir._fb.TileCoord
try:
    from typing import List
except:
    pass

class BrickVariantT(object):

    # BrickVariantT
    def __init__(
        self,
        tiles = None,
    ):
        self.tiles = tiles  # type: Optional[List[nhc.rendering.ir._fb.TileCoord.TileCoordT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        brickVariant = BrickVariant()
        brickVariant.Init(buf, pos)
        return cls.InitFromObj(brickVariant)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, brickVariant):
        x = BrickVariantT()
        x._UnPack(brickVariant)
        return x

    # BrickVariantT
    def _UnPack(self, brickVariant):
        if brickVariant is None:
            return
        if not brickVariant.TilesIsNone():
            self.tiles = []
            for i in range(brickVariant.TilesLength()):
                if brickVariant.Tiles(i) is None:
                    self.tiles.append(None)
                else:
                    tileCoord_ = nhc.rendering.ir._fb.TileCoord.TileCoordT.InitFromObj(brickVariant.Tiles(i))
                    self.tiles.append(tileCoord_)

    # BrickVariantT
    def Pack(self, builder):
        if self.tiles is not None:
            BrickVariantStartTilesVector(builder, len(self.tiles))
            for i in reversed(range(len(self.tiles))):
                self.tiles[i].Pack(builder)
            tiles = builder.EndVector()
        BrickVariantStart(builder)
        if self.tiles is not None:
            BrickVariantAddTiles(builder, tiles)
        brickVariant = BrickVariantEnd(builder)
        return brickVariant
