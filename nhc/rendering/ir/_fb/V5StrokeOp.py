# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class V5StrokeOp(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = V5StrokeOp()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsV5StrokeOp(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def V5StrokeOpBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x34", size_prefixed=size_prefixed)

    # V5StrokeOp
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # V5StrokeOp
    def RegionRef(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # V5StrokeOp
    def Outline(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from nhc.rendering.ir._fb.Outline import Outline
            obj = Outline()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # V5StrokeOp
    def WallMaterial(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from nhc.rendering.ir._fb.V5WallMaterial import V5WallMaterial
            obj = V5WallMaterial()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # V5StrokeOp
    def Cuts(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from nhc.rendering.ir._fb.Cut import Cut
            obj = Cut()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # V5StrokeOp
    def CutsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # V5StrokeOp
    def CutsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

def V5StrokeOpStart(builder):
    builder.StartObject(4)

def Start(builder):
    V5StrokeOpStart(builder)

def V5StrokeOpAddRegionRef(builder, regionRef):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(regionRef), 0)

def AddRegionRef(builder, regionRef):
    V5StrokeOpAddRegionRef(builder, regionRef)

def V5StrokeOpAddOutline(builder, outline):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(outline), 0)

def AddOutline(builder, outline):
    V5StrokeOpAddOutline(builder, outline)

def V5StrokeOpAddWallMaterial(builder, wallMaterial):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(wallMaterial), 0)

def AddWallMaterial(builder, wallMaterial):
    V5StrokeOpAddWallMaterial(builder, wallMaterial)

def V5StrokeOpAddCuts(builder, cuts):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(cuts), 0)

def AddCuts(builder, cuts):
    V5StrokeOpAddCuts(builder, cuts)

def V5StrokeOpStartCutsVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartCutsVector(builder, numElems):
    return V5StrokeOpStartCutsVector(builder, numElems)

def V5StrokeOpEnd(builder):
    return builder.EndObject()

def End(builder):
    return V5StrokeOpEnd(builder)

import nhc.rendering.ir._fb.Cut
import nhc.rendering.ir._fb.Outline
import nhc.rendering.ir._fb.V5WallMaterial
try:
    from typing import List, Optional
except:
    pass

class V5StrokeOpT(object):

    # V5StrokeOpT
    def __init__(
        self,
        regionRef = None,
        outline = None,
        wallMaterial = None,
        cuts = None,
    ):
        self.regionRef = regionRef  # type: Optional[str]
        self.outline = outline  # type: Optional[nhc.rendering.ir._fb.Outline.OutlineT]
        self.wallMaterial = wallMaterial  # type: Optional[nhc.rendering.ir._fb.V5WallMaterial.V5WallMaterialT]
        self.cuts = cuts  # type: Optional[List[nhc.rendering.ir._fb.Cut.CutT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        v5StrokeOp = V5StrokeOp()
        v5StrokeOp.Init(buf, pos)
        return cls.InitFromObj(v5StrokeOp)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, v5StrokeOp):
        x = V5StrokeOpT()
        x._UnPack(v5StrokeOp)
        return x

    # V5StrokeOpT
    def _UnPack(self, v5StrokeOp):
        if v5StrokeOp is None:
            return
        self.regionRef = v5StrokeOp.RegionRef()
        if v5StrokeOp.Outline() is not None:
            self.outline = nhc.rendering.ir._fb.Outline.OutlineT.InitFromObj(v5StrokeOp.Outline())
        if v5StrokeOp.WallMaterial() is not None:
            self.wallMaterial = nhc.rendering.ir._fb.V5WallMaterial.V5WallMaterialT.InitFromObj(v5StrokeOp.WallMaterial())
        if not v5StrokeOp.CutsIsNone():
            self.cuts = []
            for i in range(v5StrokeOp.CutsLength()):
                if v5StrokeOp.Cuts(i) is None:
                    self.cuts.append(None)
                else:
                    cut_ = nhc.rendering.ir._fb.Cut.CutT.InitFromObj(v5StrokeOp.Cuts(i))
                    self.cuts.append(cut_)

    # V5StrokeOpT
    def Pack(self, builder):
        if self.regionRef is not None:
            regionRef = builder.CreateString(self.regionRef)
        if self.outline is not None:
            outline = self.outline.Pack(builder)
        if self.wallMaterial is not None:
            wallMaterial = self.wallMaterial.Pack(builder)
        if self.cuts is not None:
            cutslist = []
            for i in range(len(self.cuts)):
                cutslist.append(self.cuts[i].Pack(builder))
            V5StrokeOpStartCutsVector(builder, len(self.cuts))
            for i in reversed(range(len(self.cuts))):
                builder.PrependUOffsetTRelative(cutslist[i])
            cuts = builder.EndVector()
        V5StrokeOpStart(builder)
        if self.regionRef is not None:
            V5StrokeOpAddRegionRef(builder, regionRef)
        if self.outline is not None:
            V5StrokeOpAddOutline(builder, outline)
        if self.wallMaterial is not None:
            V5StrokeOpAddWallMaterial(builder, wallMaterial)
        if self.cuts is not None:
            V5StrokeOpAddCuts(builder, cuts)
        v5StrokeOp = V5StrokeOpEnd(builder)
        return v5StrokeOp
