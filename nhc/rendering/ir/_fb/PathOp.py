# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class PathOp(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PathOp()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPathOp(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PathOpBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x35", size_prefixed=size_prefixed)

    # PathOp
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PathOp
    def RegionRef(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # PathOp
    def Tiles(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 8
            from nhc.rendering.ir._fb.TileCoord import TileCoord
            obj = TileCoord()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PathOp
    def TilesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PathOp
    def TilesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # PathOp
    def Style(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # PathOp
    def Seed(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

def PathOpStart(builder):
    builder.StartObject(4)

def Start(builder):
    PathOpStart(builder)

def PathOpAddRegionRef(builder, regionRef):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(regionRef), 0)

def AddRegionRef(builder, regionRef):
    PathOpAddRegionRef(builder, regionRef)

def PathOpAddTiles(builder, tiles):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(tiles), 0)

def AddTiles(builder, tiles):
    PathOpAddTiles(builder, tiles)

def PathOpStartTilesVector(builder, numElems):
    return builder.StartVector(8, numElems, 4)

def StartTilesVector(builder, numElems):
    return PathOpStartTilesVector(builder, numElems)

def PathOpAddStyle(builder, style):
    builder.PrependUint8Slot(2, style, 0)

def AddStyle(builder, style):
    PathOpAddStyle(builder, style)

def PathOpAddSeed(builder, seed):
    builder.PrependUint64Slot(3, seed, 0)

def AddSeed(builder, seed):
    PathOpAddSeed(builder, seed)

def PathOpEnd(builder):
    return builder.EndObject()

def End(builder):
    return PathOpEnd(builder)

import nhc.rendering.ir._fb.TileCoord
try:
    from typing import List
except:
    pass

class PathOpT(object):

    # PathOpT
    def __init__(
        self,
        regionRef = None,
        tiles = None,
        style = 0,
        seed = 0,
    ):
        self.regionRef = regionRef  # type: Optional[str]
        self.tiles = tiles  # type: Optional[List[nhc.rendering.ir._fb.TileCoord.TileCoordT]]
        self.style = style  # type: int
        self.seed = seed  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        pathOp = PathOp()
        pathOp.Init(buf, pos)
        return cls.InitFromObj(pathOp)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, pathOp):
        x = PathOpT()
        x._UnPack(pathOp)
        return x

    # PathOpT
    def _UnPack(self, pathOp):
        if pathOp is None:
            return
        self.regionRef = pathOp.RegionRef()
        if not pathOp.TilesIsNone():
            self.tiles = []
            for i in range(pathOp.TilesLength()):
                if pathOp.Tiles(i) is None:
                    self.tiles.append(None)
                else:
                    tileCoord_ = nhc.rendering.ir._fb.TileCoord.TileCoordT.InitFromObj(pathOp.Tiles(i))
                    self.tiles.append(tileCoord_)
        self.style = pathOp.Style()
        self.seed = pathOp.Seed()

    # PathOpT
    def Pack(self, builder):
        if self.regionRef is not None:
            regionRef = builder.CreateString(self.regionRef)
        if self.tiles is not None:
            PathOpStartTilesVector(builder, len(self.tiles))
            for i in reversed(range(len(self.tiles))):
                self.tiles[i].Pack(builder)
            tiles = builder.EndVector()
        PathOpStart(builder)
        if self.regionRef is not None:
            PathOpAddRegionRef(builder, regionRef)
        if self.tiles is not None:
            PathOpAddTiles(builder, tiles)
        PathOpAddStyle(builder, self.style)
        PathOpAddSeed(builder, self.seed)
        pathOp = PathOpEnd(builder)
        return pathOp
