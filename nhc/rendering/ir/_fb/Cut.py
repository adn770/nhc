# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class Cut(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Cut()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCut(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CutBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x35", size_prefixed=size_prefixed)

    # Cut
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Cut
    def Start(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = o + self._tab.Pos
            from nhc.rendering.ir._fb.Vec2 import Vec2
            obj = Vec2()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Cut
    def End(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = o + self._tab.Pos
            from nhc.rendering.ir._fb.Vec2 import Vec2
            obj = Vec2()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Cut
    def Style(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

def CutStart(builder):
    builder.StartObject(3)

def Start(builder):
    CutStart(builder)

def CutAddStart(builder, start):
    builder.PrependStructSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(start), 0)

def AddStart(builder, start):
    CutAddStart(builder, start)

def CutAddEnd(builder, end):
    builder.PrependStructSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(end), 0)

def AddEnd(builder, end):
    CutAddEnd(builder, end)

def CutAddStyle(builder, style):
    builder.PrependUint8Slot(2, style, 0)

def AddStyle(builder, style):
    CutAddStyle(builder, style)

def CutEnd(builder):
    return builder.EndObject()

def End(builder):
    return CutEnd(builder)

import nhc.rendering.ir._fb.Vec2
try:
    from typing import Optional
except:
    pass

class CutT(object):

    # CutT
    def __init__(
        self,
        start = None,
        end = None,
        style = 0,
    ):
        self.start = start  # type: Optional[nhc.rendering.ir._fb.Vec2.Vec2T]
        self.end = end  # type: Optional[nhc.rendering.ir._fb.Vec2.Vec2T]
        self.style = style  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        cut = Cut()
        cut.Init(buf, pos)
        return cls.InitFromObj(cut)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, cut):
        x = CutT()
        x._UnPack(cut)
        return x

    # CutT
    def _UnPack(self, cut):
        if cut is None:
            return
        if cut.Start() is not None:
            self.start = nhc.rendering.ir._fb.Vec2.Vec2T.InitFromObj(cut.Start())
        if cut.End() is not None:
            self.end = nhc.rendering.ir._fb.Vec2.Vec2T.InitFromObj(cut.End())
        self.style = cut.Style()

    # CutT
    def Pack(self, builder):
        CutStart(builder)
        if self.start is not None:
            start = self.start.Pack(builder)
            CutAddStart(builder, start)
        if self.end is not None:
            end = self.end.Pack(builder)
            CutAddEnd(builder, end)
        CutAddStyle(builder, self.style)
        cut = CutEnd(builder)
        return cut
