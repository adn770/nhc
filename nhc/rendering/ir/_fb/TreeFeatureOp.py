# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class TreeFeatureOp(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TreeFeatureOp()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTreeFeatureOp(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TreeFeatureOpBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x46", size_prefixed=size_prefixed)

    # TreeFeatureOp
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # TreeFeatureOp
    def Tiles(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 8
            from nhc.rendering.ir._fb.TileCoord import TileCoord
            obj = TileCoord()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # TreeFeatureOp
    def TilesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TreeFeatureOp
    def TilesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # TreeFeatureOp
    def Seed(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # TreeFeatureOp
    def Theme(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # TreeFeatureOp
    def Groves(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from nhc.rendering.ir._fb.GrovePolygon import GrovePolygon
            obj = GrovePolygon()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # TreeFeatureOp
    def GrovesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TreeFeatureOp
    def GrovesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

    # TreeFeatureOp
    def Groups(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # TreeFeatureOp
    def GroupsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TreeFeatureOp
    def GroupsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

def TreeFeatureOpStart(builder):
    builder.StartObject(5)

def Start(builder):
    TreeFeatureOpStart(builder)

def TreeFeatureOpAddTiles(builder, tiles):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(tiles), 0)

def AddTiles(builder, tiles):
    TreeFeatureOpAddTiles(builder, tiles)

def TreeFeatureOpStartTilesVector(builder, numElems):
    return builder.StartVector(8, numElems, 4)

def StartTilesVector(builder, numElems):
    return TreeFeatureOpStartTilesVector(builder, numElems)

def TreeFeatureOpAddSeed(builder, seed):
    builder.PrependUint64Slot(1, seed, 0)

def AddSeed(builder, seed):
    TreeFeatureOpAddSeed(builder, seed)

def TreeFeatureOpAddTheme(builder, theme):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(theme), 0)

def AddTheme(builder, theme):
    TreeFeatureOpAddTheme(builder, theme)

def TreeFeatureOpAddGroves(builder, groves):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(groves), 0)

def AddGroves(builder, groves):
    TreeFeatureOpAddGroves(builder, groves)

def TreeFeatureOpStartGrovesVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartGrovesVector(builder, numElems):
    return TreeFeatureOpStartGrovesVector(builder, numElems)

def TreeFeatureOpAddGroups(builder, groups):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(groups), 0)

def AddGroups(builder, groups):
    TreeFeatureOpAddGroups(builder, groups)

def TreeFeatureOpStartGroupsVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartGroupsVector(builder, numElems):
    return TreeFeatureOpStartGroupsVector(builder, numElems)

def TreeFeatureOpEnd(builder):
    return builder.EndObject()

def End(builder):
    return TreeFeatureOpEnd(builder)

import nhc.rendering.ir._fb.GrovePolygon
import nhc.rendering.ir._fb.TileCoord
try:
    from typing import List
except:
    pass

class TreeFeatureOpT(object):

    # TreeFeatureOpT
    def __init__(
        self,
        tiles = None,
        seed = 0,
        theme = None,
        groves = None,
        groups = None,
    ):
        self.tiles = tiles  # type: Optional[List[nhc.rendering.ir._fb.TileCoord.TileCoordT]]
        self.seed = seed  # type: int
        self.theme = theme  # type: Optional[str]
        self.groves = groves  # type: Optional[List[nhc.rendering.ir._fb.GrovePolygon.GrovePolygonT]]
        self.groups = groups  # type: Optional[List[Optional[str]]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        treeFeatureOp = TreeFeatureOp()
        treeFeatureOp.Init(buf, pos)
        return cls.InitFromObj(treeFeatureOp)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, treeFeatureOp):
        x = TreeFeatureOpT()
        x._UnPack(treeFeatureOp)
        return x

    # TreeFeatureOpT
    def _UnPack(self, treeFeatureOp):
        if treeFeatureOp is None:
            return
        if not treeFeatureOp.TilesIsNone():
            self.tiles = []
            for i in range(treeFeatureOp.TilesLength()):
                if treeFeatureOp.Tiles(i) is None:
                    self.tiles.append(None)
                else:
                    tileCoord_ = nhc.rendering.ir._fb.TileCoord.TileCoordT.InitFromObj(treeFeatureOp.Tiles(i))
                    self.tiles.append(tileCoord_)
        self.seed = treeFeatureOp.Seed()
        self.theme = treeFeatureOp.Theme()
        if not treeFeatureOp.GrovesIsNone():
            self.groves = []
            for i in range(treeFeatureOp.GrovesLength()):
                if treeFeatureOp.Groves(i) is None:
                    self.groves.append(None)
                else:
                    grovePolygon_ = nhc.rendering.ir._fb.GrovePolygon.GrovePolygonT.InitFromObj(treeFeatureOp.Groves(i))
                    self.groves.append(grovePolygon_)
        if not treeFeatureOp.GroupsIsNone():
            self.groups = []
            for i in range(treeFeatureOp.GroupsLength()):
                self.groups.append(treeFeatureOp.Groups(i))

    # TreeFeatureOpT
    def Pack(self, builder):
        if self.tiles is not None:
            TreeFeatureOpStartTilesVector(builder, len(self.tiles))
            for i in reversed(range(len(self.tiles))):
                self.tiles[i].Pack(builder)
            tiles = builder.EndVector()
        if self.theme is not None:
            theme = builder.CreateString(self.theme)
        if self.groves is not None:
            groveslist = []
            for i in range(len(self.groves)):
                groveslist.append(self.groves[i].Pack(builder))
            TreeFeatureOpStartGrovesVector(builder, len(self.groves))
            for i in reversed(range(len(self.groves))):
                builder.PrependUOffsetTRelative(groveslist[i])
            groves = builder.EndVector()
        if self.groups is not None:
            groupslist = []
            for i in range(len(self.groups)):
                groupslist.append(builder.CreateString(self.groups[i]))
            TreeFeatureOpStartGroupsVector(builder, len(self.groups))
            for i in reversed(range(len(self.groups))):
                builder.PrependUOffsetTRelative(groupslist[i])
            groups = builder.EndVector()
        TreeFeatureOpStart(builder)
        if self.tiles is not None:
            TreeFeatureOpAddTiles(builder, tiles)
        TreeFeatureOpAddSeed(builder, self.seed)
        if self.theme is not None:
            TreeFeatureOpAddTheme(builder, theme)
        if self.groves is not None:
            TreeFeatureOpAddGroves(builder, groves)
        if self.groups is not None:
            TreeFeatureOpAddGroups(builder, groups)
        treeFeatureOp = TreeFeatureOpEnd(builder)
        return treeFeatureOp
