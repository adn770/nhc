# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class PaintOp(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PaintOp()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPaintOp(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PaintOpBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x35", size_prefixed=size_prefixed)

    # PaintOp
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PaintOp
    def RegionRef(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # PaintOp
    def SubtractRegionRefs(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # PaintOp
    def SubtractRegionRefsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PaintOp
    def SubtractRegionRefsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # PaintOp
    def Material(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from nhc.rendering.ir._fb.Material import Material
            obj = Material()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def PaintOpStart(builder):
    builder.StartObject(3)

def Start(builder):
    PaintOpStart(builder)

def PaintOpAddRegionRef(builder, regionRef):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(regionRef), 0)

def AddRegionRef(builder, regionRef):
    PaintOpAddRegionRef(builder, regionRef)

def PaintOpAddSubtractRegionRefs(builder, subtractRegionRefs):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(subtractRegionRefs), 0)

def AddSubtractRegionRefs(builder, subtractRegionRefs):
    PaintOpAddSubtractRegionRefs(builder, subtractRegionRefs)

def PaintOpStartSubtractRegionRefsVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartSubtractRegionRefsVector(builder, numElems):
    return PaintOpStartSubtractRegionRefsVector(builder, numElems)

def PaintOpAddMaterial(builder, material):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(material), 0)

def AddMaterial(builder, material):
    PaintOpAddMaterial(builder, material)

def PaintOpEnd(builder):
    return builder.EndObject()

def End(builder):
    return PaintOpEnd(builder)

import nhc.rendering.ir._fb.Material
try:
    from typing import List, Optional
except:
    pass

class PaintOpT(object):

    # PaintOpT
    def __init__(
        self,
        regionRef = None,
        subtractRegionRefs = None,
        material = None,
    ):
        self.regionRef = regionRef  # type: Optional[str]
        self.subtractRegionRefs = subtractRegionRefs  # type: Optional[List[Optional[str]]]
        self.material = material  # type: Optional[nhc.rendering.ir._fb.Material.MaterialT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        paintOp = PaintOp()
        paintOp.Init(buf, pos)
        return cls.InitFromObj(paintOp)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, paintOp):
        x = PaintOpT()
        x._UnPack(paintOp)
        return x

    # PaintOpT
    def _UnPack(self, paintOp):
        if paintOp is None:
            return
        self.regionRef = paintOp.RegionRef()
        if not paintOp.SubtractRegionRefsIsNone():
            self.subtractRegionRefs = []
            for i in range(paintOp.SubtractRegionRefsLength()):
                self.subtractRegionRefs.append(paintOp.SubtractRegionRefs(i))
        if paintOp.Material() is not None:
            self.material = nhc.rendering.ir._fb.Material.MaterialT.InitFromObj(paintOp.Material())

    # PaintOpT
    def Pack(self, builder):
        if self.regionRef is not None:
            regionRef = builder.CreateString(self.regionRef)
        if self.subtractRegionRefs is not None:
            subtractRegionRefslist = []
            for i in range(len(self.subtractRegionRefs)):
                subtractRegionRefslist.append(builder.CreateString(self.subtractRegionRefs[i]))
            PaintOpStartSubtractRegionRefsVector(builder, len(self.subtractRegionRefs))
            for i in reversed(range(len(self.subtractRegionRefs))):
                builder.PrependUOffsetTRelative(subtractRegionRefslist[i])
            subtractRegionRefs = builder.EndVector()
        if self.material is not None:
            material = self.material.Pack(builder)
        PaintOpStart(builder)
        if self.regionRef is not None:
            PaintOpAddRegionRef(builder, regionRef)
        if self.subtractRegionRefs is not None:
            PaintOpAddSubtractRegionRefs(builder, subtractRegionRefs)
        if self.material is not None:
            PaintOpAddMaterial(builder, material)
        paintOp = PaintOpEnd(builder)
        return paintOp
