# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class WallMaterial(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = WallMaterial()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsWallMaterial(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def WallMaterialBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x35", size_prefixed=size_prefixed)

    # WallMaterial
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # WallMaterial
    def Family(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # WallMaterial
    def Style(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # WallMaterial
    def Treatment(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # WallMaterial
    def CornerStyle(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # WallMaterial
    def Tone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # WallMaterial
    def Seed(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

def WallMaterialStart(builder):
    builder.StartObject(6)

def Start(builder):
    WallMaterialStart(builder)

def WallMaterialAddFamily(builder, family):
    builder.PrependUint8Slot(0, family, 0)

def AddFamily(builder, family):
    WallMaterialAddFamily(builder, family)

def WallMaterialAddStyle(builder, style):
    builder.PrependUint8Slot(1, style, 0)

def AddStyle(builder, style):
    WallMaterialAddStyle(builder, style)

def WallMaterialAddTreatment(builder, treatment):
    builder.PrependUint8Slot(2, treatment, 0)

def AddTreatment(builder, treatment):
    WallMaterialAddTreatment(builder, treatment)

def WallMaterialAddCornerStyle(builder, cornerStyle):
    builder.PrependInt8Slot(3, cornerStyle, 0)

def AddCornerStyle(builder, cornerStyle):
    WallMaterialAddCornerStyle(builder, cornerStyle)

def WallMaterialAddTone(builder, tone):
    builder.PrependUint8Slot(4, tone, 0)

def AddTone(builder, tone):
    WallMaterialAddTone(builder, tone)

def WallMaterialAddSeed(builder, seed):
    builder.PrependUint64Slot(5, seed, 0)

def AddSeed(builder, seed):
    WallMaterialAddSeed(builder, seed)

def WallMaterialEnd(builder):
    return builder.EndObject()

def End(builder):
    return WallMaterialEnd(builder)


class WallMaterialT(object):

    # WallMaterialT
    def __init__(
        self,
        family = 0,
        style = 0,
        treatment = 0,
        cornerStyle = 0,
        tone = 0,
        seed = 0,
    ):
        self.family = family  # type: int
        self.style = style  # type: int
        self.treatment = treatment  # type: int
        self.cornerStyle = cornerStyle  # type: int
        self.tone = tone  # type: int
        self.seed = seed  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        wallMaterial = WallMaterial()
        wallMaterial.Init(buf, pos)
        return cls.InitFromObj(wallMaterial)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, wallMaterial):
        x = WallMaterialT()
        x._UnPack(wallMaterial)
        return x

    # WallMaterialT
    def _UnPack(self, wallMaterial):
        if wallMaterial is None:
            return
        self.family = wallMaterial.Family()
        self.style = wallMaterial.Style()
        self.treatment = wallMaterial.Treatment()
        self.cornerStyle = wallMaterial.CornerStyle()
        self.tone = wallMaterial.Tone()
        self.seed = wallMaterial.Seed()

    # WallMaterialT
    def Pack(self, builder):
        WallMaterialStart(builder)
        WallMaterialAddFamily(builder, self.family)
        WallMaterialAddStyle(builder, self.style)
        WallMaterialAddTreatment(builder, self.treatment)
        WallMaterialAddCornerStyle(builder, self.cornerStyle)
        WallMaterialAddTone(builder, self.tone)
        WallMaterialAddSeed(builder, self.seed)
        wallMaterial = WallMaterialEnd(builder)
        return wallMaterial
