# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class TerrainTintOp(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TerrainTintOp()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTerrainTintOp(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TerrainTintOpBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x46", size_prefixed=size_prefixed)

    # TerrainTintOp
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # TerrainTintOp
    def Tiles(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 12
            from nhc.rendering.ir._fb.TerrainTintTile import TerrainTintTile
            obj = TerrainTintTile()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # TerrainTintOp
    def TilesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TerrainTintOp
    def TilesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # TerrainTintOp
    def RoomWashes(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from nhc.rendering.ir._fb.RoomWash import RoomWash
            obj = RoomWash()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # TerrainTintOp
    def RoomWashesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TerrainTintOp
    def RoomWashesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # TerrainTintOp
    def ClipRegion(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def TerrainTintOpStart(builder):
    builder.StartObject(3)

def Start(builder):
    TerrainTintOpStart(builder)

def TerrainTintOpAddTiles(builder, tiles):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(tiles), 0)

def AddTiles(builder, tiles):
    TerrainTintOpAddTiles(builder, tiles)

def TerrainTintOpStartTilesVector(builder, numElems):
    return builder.StartVector(12, numElems, 4)

def StartTilesVector(builder, numElems):
    return TerrainTintOpStartTilesVector(builder, numElems)

def TerrainTintOpAddRoomWashes(builder, roomWashes):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(roomWashes), 0)

def AddRoomWashes(builder, roomWashes):
    TerrainTintOpAddRoomWashes(builder, roomWashes)

def TerrainTintOpStartRoomWashesVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRoomWashesVector(builder, numElems):
    return TerrainTintOpStartRoomWashesVector(builder, numElems)

def TerrainTintOpAddClipRegion(builder, clipRegion):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(clipRegion), 0)

def AddClipRegion(builder, clipRegion):
    TerrainTintOpAddClipRegion(builder, clipRegion)

def TerrainTintOpEnd(builder):
    return builder.EndObject()

def End(builder):
    return TerrainTintOpEnd(builder)

import nhc.rendering.ir._fb.RoomWash
import nhc.rendering.ir._fb.TerrainTintTile
try:
    from typing import List
except:
    pass

class TerrainTintOpT(object):

    # TerrainTintOpT
    def __init__(
        self,
        tiles = None,
        roomWashes = None,
        clipRegion = None,
    ):
        self.tiles = tiles  # type: Optional[List[nhc.rendering.ir._fb.TerrainTintTile.TerrainTintTileT]]
        self.roomWashes = roomWashes  # type: Optional[List[nhc.rendering.ir._fb.RoomWash.RoomWashT]]
        self.clipRegion = clipRegion  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        terrainTintOp = TerrainTintOp()
        terrainTintOp.Init(buf, pos)
        return cls.InitFromObj(terrainTintOp)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, terrainTintOp):
        x = TerrainTintOpT()
        x._UnPack(terrainTintOp)
        return x

    # TerrainTintOpT
    def _UnPack(self, terrainTintOp):
        if terrainTintOp is None:
            return
        if not terrainTintOp.TilesIsNone():
            self.tiles = []
            for i in range(terrainTintOp.TilesLength()):
                if terrainTintOp.Tiles(i) is None:
                    self.tiles.append(None)
                else:
                    terrainTintTile_ = nhc.rendering.ir._fb.TerrainTintTile.TerrainTintTileT.InitFromObj(terrainTintOp.Tiles(i))
                    self.tiles.append(terrainTintTile_)
        if not terrainTintOp.RoomWashesIsNone():
            self.roomWashes = []
            for i in range(terrainTintOp.RoomWashesLength()):
                if terrainTintOp.RoomWashes(i) is None:
                    self.roomWashes.append(None)
                else:
                    roomWash_ = nhc.rendering.ir._fb.RoomWash.RoomWashT.InitFromObj(terrainTintOp.RoomWashes(i))
                    self.roomWashes.append(roomWash_)
        self.clipRegion = terrainTintOp.ClipRegion()

    # TerrainTintOpT
    def Pack(self, builder):
        if self.tiles is not None:
            TerrainTintOpStartTilesVector(builder, len(self.tiles))
            for i in reversed(range(len(self.tiles))):
                self.tiles[i].Pack(builder)
            tiles = builder.EndVector()
        if self.roomWashes is not None:
            roomWasheslist = []
            for i in range(len(self.roomWashes)):
                roomWasheslist.append(self.roomWashes[i].Pack(builder))
            TerrainTintOpStartRoomWashesVector(builder, len(self.roomWashes))
            for i in reversed(range(len(self.roomWashes))):
                builder.PrependUOffsetTRelative(roomWasheslist[i])
            roomWashes = builder.EndVector()
        if self.clipRegion is not None:
            clipRegion = builder.CreateString(self.clipRegion)
        TerrainTintOpStart(builder)
        if self.tiles is not None:
            TerrainTintOpAddTiles(builder, tiles)
        if self.roomWashes is not None:
            TerrainTintOpAddRoomWashes(builder, roomWashes)
        if self.clipRegion is not None:
            TerrainTintOpAddClipRegion(builder, clipRegion)
        terrainTintOp = TerrainTintOpEnd(builder)
        return terrainTintOp
