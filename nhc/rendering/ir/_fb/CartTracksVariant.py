# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class CartTracksVariant(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CartTracksVariant()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCartTracksVariant(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CartTracksVariantBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x46", size_prefixed=size_prefixed)

    # CartTracksVariant
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # CartTracksVariant
    def Tiles(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 8
            from nhc.rendering.ir._fb.TileCoord import TileCoord
            obj = TileCoord()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # CartTracksVariant
    def TilesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CartTracksVariant
    def TilesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def CartTracksVariantStart(builder):
    builder.StartObject(1)

def Start(builder):
    CartTracksVariantStart(builder)

def CartTracksVariantAddTiles(builder, tiles):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(tiles), 0)

def AddTiles(builder, tiles):
    CartTracksVariantAddTiles(builder, tiles)

def CartTracksVariantStartTilesVector(builder, numElems):
    return builder.StartVector(8, numElems, 4)

def StartTilesVector(builder, numElems):
    return CartTracksVariantStartTilesVector(builder, numElems)

def CartTracksVariantEnd(builder):
    return builder.EndObject()

def End(builder):
    return CartTracksVariantEnd(builder)

import nhc.rendering.ir._fb.TileCoord
try:
    from typing import List
except:
    pass

class CartTracksVariantT(object):

    # CartTracksVariantT
    def __init__(
        self,
        tiles = None,
    ):
        self.tiles = tiles  # type: Optional[List[nhc.rendering.ir._fb.TileCoord.TileCoordT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        cartTracksVariant = CartTracksVariant()
        cartTracksVariant.Init(buf, pos)
        return cls.InitFromObj(cartTracksVariant)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, cartTracksVariant):
        x = CartTracksVariantT()
        x._UnPack(cartTracksVariant)
        return x

    # CartTracksVariantT
    def _UnPack(self, cartTracksVariant):
        if cartTracksVariant is None:
            return
        if not cartTracksVariant.TilesIsNone():
            self.tiles = []
            for i in range(cartTracksVariant.TilesLength()):
                if cartTracksVariant.Tiles(i) is None:
                    self.tiles.append(None)
                else:
                    tileCoord_ = nhc.rendering.ir._fb.TileCoord.TileCoordT.InitFromObj(cartTracksVariant.Tiles(i))
                    self.tiles.append(tileCoord_)

    # CartTracksVariantT
    def Pack(self, builder):
        if self.tiles is not None:
            CartTracksVariantStartTilesVector(builder, len(self.tiles))
            for i in reversed(range(len(self.tiles))):
                self.tiles[i].Pack(builder)
            tiles = builder.EndVector()
        CartTracksVariantStart(builder)
        if self.tiles is not None:
            CartTracksVariantAddTiles(builder, tiles)
        cartTracksVariant = CartTracksVariantEnd(builder)
        return cartTracksVariant
