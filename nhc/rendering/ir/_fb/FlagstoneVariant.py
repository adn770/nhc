# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class FlagstoneVariant(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = FlagstoneVariant()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsFlagstoneVariant(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def FlagstoneVariantBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x33", size_prefixed=size_prefixed)

    # FlagstoneVariant
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # FlagstoneVariant
    def Tiles(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 8
            from nhc.rendering.ir._fb.TileCoord import TileCoord
            obj = TileCoord()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # FlagstoneVariant
    def TilesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FlagstoneVariant
    def TilesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def FlagstoneVariantStart(builder):
    builder.StartObject(1)

def Start(builder):
    FlagstoneVariantStart(builder)

def FlagstoneVariantAddTiles(builder, tiles):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(tiles), 0)

def AddTiles(builder, tiles):
    FlagstoneVariantAddTiles(builder, tiles)

def FlagstoneVariantStartTilesVector(builder, numElems):
    return builder.StartVector(8, numElems, 4)

def StartTilesVector(builder, numElems):
    return FlagstoneVariantStartTilesVector(builder, numElems)

def FlagstoneVariantEnd(builder):
    return builder.EndObject()

def End(builder):
    return FlagstoneVariantEnd(builder)

import nhc.rendering.ir._fb.TileCoord
try:
    from typing import List
except:
    pass

class FlagstoneVariantT(object):

    # FlagstoneVariantT
    def __init__(
        self,
        tiles = None,
    ):
        self.tiles = tiles  # type: Optional[List[nhc.rendering.ir._fb.TileCoord.TileCoordT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        flagstoneVariant = FlagstoneVariant()
        flagstoneVariant.Init(buf, pos)
        return cls.InitFromObj(flagstoneVariant)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, flagstoneVariant):
        x = FlagstoneVariantT()
        x._UnPack(flagstoneVariant)
        return x

    # FlagstoneVariantT
    def _UnPack(self, flagstoneVariant):
        if flagstoneVariant is None:
            return
        if not flagstoneVariant.TilesIsNone():
            self.tiles = []
            for i in range(flagstoneVariant.TilesLength()):
                if flagstoneVariant.Tiles(i) is None:
                    self.tiles.append(None)
                else:
                    tileCoord_ = nhc.rendering.ir._fb.TileCoord.TileCoordT.InitFromObj(flagstoneVariant.Tiles(i))
                    self.tiles.append(tileCoord_)

    # FlagstoneVariantT
    def Pack(self, builder):
        if self.tiles is not None:
            FlagstoneVariantStartTilesVector(builder, len(self.tiles))
            for i in reversed(range(len(self.tiles))):
                self.tiles[i].Pack(builder)
            tiles = builder.EndVector()
        FlagstoneVariantStart(builder)
        if self.tiles is not None:
            FlagstoneVariantAddTiles(builder, tiles)
        flagstoneVariant = FlagstoneVariantEnd(builder)
        return flagstoneVariant
