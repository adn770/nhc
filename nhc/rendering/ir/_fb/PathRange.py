# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class PathRange(object):
    __slots__ = ['_tab']

    @classmethod
    def SizeOf(cls):
        return 12

    # PathRange
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PathRange
    def Start(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0))
    # PathRange
    def Count(self): return self._tab.Get(flatbuffers.number_types.Uint32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(4))
    # PathRange
    def IsHole(self): return self._tab.Get(flatbuffers.number_types.BoolFlags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(8))

def CreatePathRange(builder, start, count, isHole):
    builder.Prep(4, 12)
    builder.Pad(3)
    builder.PrependBool(isHole)
    builder.PrependUint32(count)
    builder.PrependUint32(start)
    return builder.Offset()


class PathRangeT(object):

    # PathRangeT
    def __init__(
        self,
        start = 0,
        count = 0,
        isHole = False,
    ):
        self.start = start  # type: int
        self.count = count  # type: int
        self.isHole = isHole  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        pathRange = PathRange()
        pathRange.Init(buf, pos)
        return cls.InitFromObj(pathRange)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, pathRange):
        x = PathRangeT()
        x._UnPack(pathRange)
        return x

    # PathRangeT
    def _UnPack(self, pathRange):
        if pathRange is None:
            return
        self.start = pathRange.Start()
        self.count = pathRange.Count()
        self.isHole = pathRange.IsHole()

    # PathRangeT
    def Pack(self, builder):
        return CreatePathRange(builder, self.start, self.count, self.isHole)
