# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class FloorIR(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = FloorIR()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsFloorIR(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def FloorIRBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x35", size_prefixed=size_prefixed)

    # FloorIR
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # FloorIR
    def Major(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # FloorIR
    def Minor(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # FloorIR
    def WidthTiles(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # FloorIR
    def HeightTiles(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # FloorIR
    def Cell(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 32

    # FloorIR
    def Padding(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 32

    # FloorIR
    def BaseSeed(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # FloorIR
    def Regions(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from nhc.rendering.ir._fb.Region import Region
            obj = Region()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # FloorIR
    def RegionsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FloorIR
    def RegionsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

    # FloorIR
    def Ops(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from nhc.rendering.ir._fb.OpEntry import OpEntry
            obj = OpEntry()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # FloorIR
    def OpsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FloorIR
    def OpsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        return o == 0

def FloorIRStart(builder):
    builder.StartObject(9)

def Start(builder):
    FloorIRStart(builder)

def FloorIRAddMajor(builder, major):
    builder.PrependUint32Slot(0, major, 0)

def AddMajor(builder, major):
    FloorIRAddMajor(builder, major)

def FloorIRAddMinor(builder, minor):
    builder.PrependUint32Slot(1, minor, 0)

def AddMinor(builder, minor):
    FloorIRAddMinor(builder, minor)

def FloorIRAddWidthTiles(builder, widthTiles):
    builder.PrependUint32Slot(2, widthTiles, 0)

def AddWidthTiles(builder, widthTiles):
    FloorIRAddWidthTiles(builder, widthTiles)

def FloorIRAddHeightTiles(builder, heightTiles):
    builder.PrependUint32Slot(3, heightTiles, 0)

def AddHeightTiles(builder, heightTiles):
    FloorIRAddHeightTiles(builder, heightTiles)

def FloorIRAddCell(builder, cell):
    builder.PrependUint32Slot(4, cell, 32)

def AddCell(builder, cell):
    FloorIRAddCell(builder, cell)

def FloorIRAddPadding(builder, padding):
    builder.PrependUint32Slot(5, padding, 32)

def AddPadding(builder, padding):
    FloorIRAddPadding(builder, padding)

def FloorIRAddBaseSeed(builder, baseSeed):
    builder.PrependUint64Slot(6, baseSeed, 0)

def AddBaseSeed(builder, baseSeed):
    FloorIRAddBaseSeed(builder, baseSeed)

def FloorIRAddRegions(builder, regions):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(regions), 0)

def AddRegions(builder, regions):
    FloorIRAddRegions(builder, regions)

def FloorIRStartRegionsVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRegionsVector(builder, numElems):
    return FloorIRStartRegionsVector(builder, numElems)

def FloorIRAddOps(builder, ops):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(ops), 0)

def AddOps(builder, ops):
    FloorIRAddOps(builder, ops)

def FloorIRStartOpsVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartOpsVector(builder, numElems):
    return FloorIRStartOpsVector(builder, numElems)

def FloorIREnd(builder):
    return builder.EndObject()

def End(builder):
    return FloorIREnd(builder)

import nhc.rendering.ir._fb.OpEntry
import nhc.rendering.ir._fb.Region
try:
    from typing import List
except:
    pass

class FloorIRT(object):

    # FloorIRT
    def __init__(
        self,
        major = 0,
        minor = 0,
        widthTiles = 0,
        heightTiles = 0,
        cell = 32,
        padding = 32,
        baseSeed = 0,
        regions = None,
        ops = None,
    ):
        self.major = major  # type: int
        self.minor = minor  # type: int
        self.widthTiles = widthTiles  # type: int
        self.heightTiles = heightTiles  # type: int
        self.cell = cell  # type: int
        self.padding = padding  # type: int
        self.baseSeed = baseSeed  # type: int
        self.regions = regions  # type: Optional[List[nhc.rendering.ir._fb.Region.RegionT]]
        self.ops = ops  # type: Optional[List[nhc.rendering.ir._fb.OpEntry.OpEntryT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        floorIr = FloorIR()
        floorIr.Init(buf, pos)
        return cls.InitFromObj(floorIr)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, floorIr):
        x = FloorIRT()
        x._UnPack(floorIr)
        return x

    # FloorIRT
    def _UnPack(self, floorIr):
        if floorIr is None:
            return
        self.major = floorIr.Major()
        self.minor = floorIr.Minor()
        self.widthTiles = floorIr.WidthTiles()
        self.heightTiles = floorIr.HeightTiles()
        self.cell = floorIr.Cell()
        self.padding = floorIr.Padding()
        self.baseSeed = floorIr.BaseSeed()
        if not floorIr.RegionsIsNone():
            self.regions = []
            for i in range(floorIr.RegionsLength()):
                if floorIr.Regions(i) is None:
                    self.regions.append(None)
                else:
                    region_ = nhc.rendering.ir._fb.Region.RegionT.InitFromObj(floorIr.Regions(i))
                    self.regions.append(region_)
        if not floorIr.OpsIsNone():
            self.ops = []
            for i in range(floorIr.OpsLength()):
                if floorIr.Ops(i) is None:
                    self.ops.append(None)
                else:
                    opEntry_ = nhc.rendering.ir._fb.OpEntry.OpEntryT.InitFromObj(floorIr.Ops(i))
                    self.ops.append(opEntry_)

    # FloorIRT
    def Pack(self, builder):
        if self.regions is not None:
            regionslist = []
            for i in range(len(self.regions)):
                regionslist.append(self.regions[i].Pack(builder))
            FloorIRStartRegionsVector(builder, len(self.regions))
            for i in reversed(range(len(self.regions))):
                builder.PrependUOffsetTRelative(regionslist[i])
            regions = builder.EndVector()
        if self.ops is not None:
            opslist = []
            for i in range(len(self.ops)):
                opslist.append(self.ops[i].Pack(builder))
            FloorIRStartOpsVector(builder, len(self.ops))
            for i in reversed(range(len(self.ops))):
                builder.PrependUOffsetTRelative(opslist[i])
            ops = builder.EndVector()
        FloorIRStart(builder)
        FloorIRAddMajor(builder, self.major)
        FloorIRAddMinor(builder, self.minor)
        FloorIRAddWidthTiles(builder, self.widthTiles)
        FloorIRAddHeightTiles(builder, self.heightTiles)
        FloorIRAddCell(builder, self.cell)
        FloorIRAddPadding(builder, self.padding)
        FloorIRAddBaseSeed(builder, self.baseSeed)
        if self.regions is not None:
            FloorIRAddRegions(builder, regions)
        if self.ops is not None:
            FloorIRAddOps(builder, ops)
        floorIr = FloorIREnd(builder)
        return floorIr
