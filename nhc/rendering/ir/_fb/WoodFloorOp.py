# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class WoodFloorOp(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = WoodFloorOp()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsWoodFloorOp(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def WoodFloorOpBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x46", size_prefixed=size_prefixed)

    # WoodFloorOp
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # WoodFloorOp
    def Tiles(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 8
            from nhc.rendering.ir._fb.TileCoord import TileCoord
            obj = TileCoord()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # WoodFloorOp
    def TilesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # WoodFloorOp
    def TilesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # WoodFloorOp
    def Seed(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # WoodFloorOp
    def Theme(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # WoodFloorOp
    def BuildingPolygon(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 8
            from nhc.rendering.ir._fb.Vec2 import Vec2
            obj = Vec2()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # WoodFloorOp
    def BuildingPolygonLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # WoodFloorOp
    def BuildingPolygonIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

def WoodFloorOpStart(builder):
    builder.StartObject(4)

def Start(builder):
    WoodFloorOpStart(builder)

def WoodFloorOpAddTiles(builder, tiles):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(tiles), 0)

def AddTiles(builder, tiles):
    WoodFloorOpAddTiles(builder, tiles)

def WoodFloorOpStartTilesVector(builder, numElems):
    return builder.StartVector(8, numElems, 4)

def StartTilesVector(builder, numElems):
    return WoodFloorOpStartTilesVector(builder, numElems)

def WoodFloorOpAddSeed(builder, seed):
    builder.PrependUint64Slot(1, seed, 0)

def AddSeed(builder, seed):
    WoodFloorOpAddSeed(builder, seed)

def WoodFloorOpAddTheme(builder, theme):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(theme), 0)

def AddTheme(builder, theme):
    WoodFloorOpAddTheme(builder, theme)

def WoodFloorOpAddBuildingPolygon(builder, buildingPolygon):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(buildingPolygon), 0)

def AddBuildingPolygon(builder, buildingPolygon):
    WoodFloorOpAddBuildingPolygon(builder, buildingPolygon)

def WoodFloorOpStartBuildingPolygonVector(builder, numElems):
    return builder.StartVector(8, numElems, 4)

def StartBuildingPolygonVector(builder, numElems):
    return WoodFloorOpStartBuildingPolygonVector(builder, numElems)

def WoodFloorOpEnd(builder):
    return builder.EndObject()

def End(builder):
    return WoodFloorOpEnd(builder)

import nhc.rendering.ir._fb.TileCoord
import nhc.rendering.ir._fb.Vec2
try:
    from typing import List
except:
    pass

class WoodFloorOpT(object):

    # WoodFloorOpT
    def __init__(
        self,
        tiles = None,
        seed = 0,
        theme = None,
        buildingPolygon = None,
    ):
        self.tiles = tiles  # type: Optional[List[nhc.rendering.ir._fb.TileCoord.TileCoordT]]
        self.seed = seed  # type: int
        self.theme = theme  # type: Optional[str]
        self.buildingPolygon = buildingPolygon  # type: Optional[List[nhc.rendering.ir._fb.Vec2.Vec2T]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        woodFloorOp = WoodFloorOp()
        woodFloorOp.Init(buf, pos)
        return cls.InitFromObj(woodFloorOp)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, woodFloorOp):
        x = WoodFloorOpT()
        x._UnPack(woodFloorOp)
        return x

    # WoodFloorOpT
    def _UnPack(self, woodFloorOp):
        if woodFloorOp is None:
            return
        if not woodFloorOp.TilesIsNone():
            self.tiles = []
            for i in range(woodFloorOp.TilesLength()):
                if woodFloorOp.Tiles(i) is None:
                    self.tiles.append(None)
                else:
                    tileCoord_ = nhc.rendering.ir._fb.TileCoord.TileCoordT.InitFromObj(woodFloorOp.Tiles(i))
                    self.tiles.append(tileCoord_)
        self.seed = woodFloorOp.Seed()
        self.theme = woodFloorOp.Theme()
        if not woodFloorOp.BuildingPolygonIsNone():
            self.buildingPolygon = []
            for i in range(woodFloorOp.BuildingPolygonLength()):
                if woodFloorOp.BuildingPolygon(i) is None:
                    self.buildingPolygon.append(None)
                else:
                    vec2_ = nhc.rendering.ir._fb.Vec2.Vec2T.InitFromObj(woodFloorOp.BuildingPolygon(i))
                    self.buildingPolygon.append(vec2_)

    # WoodFloorOpT
    def Pack(self, builder):
        if self.tiles is not None:
            WoodFloorOpStartTilesVector(builder, len(self.tiles))
            for i in reversed(range(len(self.tiles))):
                self.tiles[i].Pack(builder)
            tiles = builder.EndVector()
        if self.theme is not None:
            theme = builder.CreateString(self.theme)
        if self.buildingPolygon is not None:
            WoodFloorOpStartBuildingPolygonVector(builder, len(self.buildingPolygon))
            for i in reversed(range(len(self.buildingPolygon))):
                self.buildingPolygon[i].Pack(builder)
            buildingPolygon = builder.EndVector()
        WoodFloorOpStart(builder)
        if self.tiles is not None:
            WoodFloorOpAddTiles(builder, tiles)
        WoodFloorOpAddSeed(builder, self.seed)
        if self.theme is not None:
            WoodFloorOpAddTheme(builder, theme)
        if self.buildingPolygon is not None:
            WoodFloorOpAddBuildingPolygon(builder, buildingPolygon)
        woodFloorOp = WoodFloorOpEnd(builder)
        return woodFloorOp
