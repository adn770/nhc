# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class Material(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Material()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsMaterial(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def MaterialBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x35", size_prefixed=size_prefixed)

    # Material
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Material
    def Family(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Material
    def Style(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Material
    def SubPattern(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Material
    def Tone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Material
    def Seed(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

def MaterialStart(builder):
    builder.StartObject(5)

def Start(builder):
    MaterialStart(builder)

def MaterialAddFamily(builder, family):
    builder.PrependUint8Slot(0, family, 0)

def AddFamily(builder, family):
    MaterialAddFamily(builder, family)

def MaterialAddStyle(builder, style):
    builder.PrependUint8Slot(1, style, 0)

def AddStyle(builder, style):
    MaterialAddStyle(builder, style)

def MaterialAddSubPattern(builder, subPattern):
    builder.PrependUint8Slot(2, subPattern, 0)

def AddSubPattern(builder, subPattern):
    MaterialAddSubPattern(builder, subPattern)

def MaterialAddTone(builder, tone):
    builder.PrependUint8Slot(3, tone, 0)

def AddTone(builder, tone):
    MaterialAddTone(builder, tone)

def MaterialAddSeed(builder, seed):
    builder.PrependUint64Slot(4, seed, 0)

def AddSeed(builder, seed):
    MaterialAddSeed(builder, seed)

def MaterialEnd(builder):
    return builder.EndObject()

def End(builder):
    return MaterialEnd(builder)


class MaterialT(object):

    # MaterialT
    def __init__(
        self,
        family = 0,
        style = 0,
        subPattern = 0,
        tone = 0,
        seed = 0,
    ):
        self.family = family  # type: int
        self.style = style  # type: int
        self.subPattern = subPattern  # type: int
        self.tone = tone  # type: int
        self.seed = seed  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        material = Material()
        material.Init(buf, pos)
        return cls.InitFromObj(material)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, material):
        x = MaterialT()
        x._UnPack(material)
        return x

    # MaterialT
    def _UnPack(self, material):
        if material is None:
            return
        self.family = material.Family()
        self.style = material.Style()
        self.subPattern = material.SubPattern()
        self.tone = material.Tone()
        self.seed = material.Seed()

    # MaterialT
    def Pack(self, builder):
        MaterialStart(builder)
        MaterialAddFamily(builder, self.family)
        MaterialAddStyle(builder, self.style)
        MaterialAddSubPattern(builder, self.subPattern)
        MaterialAddTone(builder, self.tone)
        MaterialAddSeed(builder, self.seed)
        material = MaterialEnd(builder)
        return material
