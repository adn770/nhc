# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class TileCoord(object):
    __slots__ = ['_tab']

    @classmethod
    def SizeOf(cls):
        return 8

    # TileCoord
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # TileCoord
    def X(self): return self._tab.Get(flatbuffers.number_types.Int32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0))
    # TileCoord
    def Y(self): return self._tab.Get(flatbuffers.number_types.Int32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(4))

def CreateTileCoord(builder, x, y):
    builder.Prep(4, 8)
    builder.PrependInt32(y)
    builder.PrependInt32(x)
    return builder.Offset()


class TileCoordT(object):

    # TileCoordT
    def __init__(
        self,
        x = 0,
        y = 0,
    ):
        self.x = x  # type: int
        self.y = y  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        tileCoord = TileCoord()
        tileCoord.Init(buf, pos)
        return cls.InitFromObj(tileCoord)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, tileCoord):
        x = TileCoordT()
        x._UnPack(tileCoord)
        return x

    # TileCoordT
    def _UnPack(self, tileCoord):
        if tileCoord is None:
            return
        self.x = tileCoord.X()
        self.y = tileCoord.Y()

    # TileCoordT
    def Pack(self, builder):
        return CreateTileCoord(builder, self.x, self.y)
