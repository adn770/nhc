# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class Polygon(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Polygon()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPolygon(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PolygonBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x35", size_prefixed=size_prefixed)

    # Polygon
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Polygon
    def Paths(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 8
            from nhc.rendering.ir._fb.Vec2 import Vec2
            obj = Vec2()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Polygon
    def PathsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # Polygon
    def PathsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # Polygon
    def Rings(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 12
            from nhc.rendering.ir._fb.PathRange import PathRange
            obj = PathRange()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Polygon
    def RingsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # Polygon
    def RingsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

def PolygonStart(builder):
    builder.StartObject(2)

def Start(builder):
    PolygonStart(builder)

def PolygonAddPaths(builder, paths):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(paths), 0)

def AddPaths(builder, paths):
    PolygonAddPaths(builder, paths)

def PolygonStartPathsVector(builder, numElems):
    return builder.StartVector(8, numElems, 4)

def StartPathsVector(builder, numElems):
    return PolygonStartPathsVector(builder, numElems)

def PolygonAddRings(builder, rings):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(rings), 0)

def AddRings(builder, rings):
    PolygonAddRings(builder, rings)

def PolygonStartRingsVector(builder, numElems):
    return builder.StartVector(12, numElems, 4)

def StartRingsVector(builder, numElems):
    return PolygonStartRingsVector(builder, numElems)

def PolygonEnd(builder):
    return builder.EndObject()

def End(builder):
    return PolygonEnd(builder)

import nhc.rendering.ir._fb.PathRange
import nhc.rendering.ir._fb.Vec2
try:
    from typing import List
except:
    pass

class PolygonT(object):

    # PolygonT
    def __init__(
        self,
        paths = None,
        rings = None,
    ):
        self.paths = paths  # type: Optional[List[nhc.rendering.ir._fb.Vec2.Vec2T]]
        self.rings = rings  # type: Optional[List[nhc.rendering.ir._fb.PathRange.PathRangeT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        polygon = Polygon()
        polygon.Init(buf, pos)
        return cls.InitFromObj(polygon)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, polygon):
        x = PolygonT()
        x._UnPack(polygon)
        return x

    # PolygonT
    def _UnPack(self, polygon):
        if polygon is None:
            return
        if not polygon.PathsIsNone():
            self.paths = []
            for i in range(polygon.PathsLength()):
                if polygon.Paths(i) is None:
                    self.paths.append(None)
                else:
                    vec2_ = nhc.rendering.ir._fb.Vec2.Vec2T.InitFromObj(polygon.Paths(i))
                    self.paths.append(vec2_)
        if not polygon.RingsIsNone():
            self.rings = []
            for i in range(polygon.RingsLength()):
                if polygon.Rings(i) is None:
                    self.rings.append(None)
                else:
                    pathRange_ = nhc.rendering.ir._fb.PathRange.PathRangeT.InitFromObj(polygon.Rings(i))
                    self.rings.append(pathRange_)

    # PolygonT
    def Pack(self, builder):
        if self.paths is not None:
            PolygonStartPathsVector(builder, len(self.paths))
            for i in reversed(range(len(self.paths))):
                self.paths[i].Pack(builder)
            paths = builder.EndVector()
        if self.rings is not None:
            PolygonStartRingsVector(builder, len(self.rings))
            for i in reversed(range(len(self.rings))):
                self.rings[i].Pack(builder)
            rings = builder.EndVector()
        PolygonStart(builder)
        if self.paths is not None:
            PolygonAddPaths(builder, paths)
        if self.rings is not None:
            PolygonAddRings(builder, rings)
        polygon = PolygonEnd(builder)
        return polygon
