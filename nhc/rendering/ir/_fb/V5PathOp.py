# automatically generated by the FlatBuffers compiler, do not modify

# namespace: _fb

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class V5PathOp(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = V5PathOp()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsV5PathOp(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def V5PathOpBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x49\x52\x34", size_prefixed=size_prefixed)

    # V5PathOp
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # V5PathOp
    def RegionRef(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # V5PathOp
    def Tiles(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 8
            from nhc.rendering.ir._fb.TileCoord import TileCoord
            obj = TileCoord()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # V5PathOp
    def TilesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # V5PathOp
    def TilesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # V5PathOp
    def Style(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # V5PathOp
    def Seed(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

def V5PathOpStart(builder):
    builder.StartObject(4)

def Start(builder):
    V5PathOpStart(builder)

def V5PathOpAddRegionRef(builder, regionRef):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(regionRef), 0)

def AddRegionRef(builder, regionRef):
    V5PathOpAddRegionRef(builder, regionRef)

def V5PathOpAddTiles(builder, tiles):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(tiles), 0)

def AddTiles(builder, tiles):
    V5PathOpAddTiles(builder, tiles)

def V5PathOpStartTilesVector(builder, numElems):
    return builder.StartVector(8, numElems, 4)

def StartTilesVector(builder, numElems):
    return V5PathOpStartTilesVector(builder, numElems)

def V5PathOpAddStyle(builder, style):
    builder.PrependUint8Slot(2, style, 0)

def AddStyle(builder, style):
    V5PathOpAddStyle(builder, style)

def V5PathOpAddSeed(builder, seed):
    builder.PrependUint64Slot(3, seed, 0)

def AddSeed(builder, seed):
    V5PathOpAddSeed(builder, seed)

def V5PathOpEnd(builder):
    return builder.EndObject()

def End(builder):
    return V5PathOpEnd(builder)

import nhc.rendering.ir._fb.TileCoord
try:
    from typing import List
except:
    pass

class V5PathOpT(object):

    # V5PathOpT
    def __init__(
        self,
        regionRef = None,
        tiles = None,
        style = 0,
        seed = 0,
    ):
        self.regionRef = regionRef  # type: Optional[str]
        self.tiles = tiles  # type: Optional[List[nhc.rendering.ir._fb.TileCoord.TileCoordT]]
        self.style = style  # type: int
        self.seed = seed  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        v5PathOp = V5PathOp()
        v5PathOp.Init(buf, pos)
        return cls.InitFromObj(v5PathOp)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, v5PathOp):
        x = V5PathOpT()
        x._UnPack(v5PathOp)
        return x

    # V5PathOpT
    def _UnPack(self, v5PathOp):
        if v5PathOp is None:
            return
        self.regionRef = v5PathOp.RegionRef()
        if not v5PathOp.TilesIsNone():
            self.tiles = []
            for i in range(v5PathOp.TilesLength()):
                if v5PathOp.Tiles(i) is None:
                    self.tiles.append(None)
                else:
                    tileCoord_ = nhc.rendering.ir._fb.TileCoord.TileCoordT.InitFromObj(v5PathOp.Tiles(i))
                    self.tiles.append(tileCoord_)
        self.style = v5PathOp.Style()
        self.seed = v5PathOp.Seed()

    # V5PathOpT
    def Pack(self, builder):
        if self.regionRef is not None:
            regionRef = builder.CreateString(self.regionRef)
        if self.tiles is not None:
            V5PathOpStartTilesVector(builder, len(self.tiles))
            for i in reversed(range(len(self.tiles))):
                self.tiles[i].Pack(builder)
            tiles = builder.EndVector()
        V5PathOpStart(builder)
        if self.regionRef is not None:
            V5PathOpAddRegionRef(builder, regionRef)
        if self.tiles is not None:
            V5PathOpAddTiles(builder, tiles)
        V5PathOpAddStyle(builder, self.style)
        V5PathOpAddSeed(builder, self.seed)
        v5PathOp = V5PathOpEnd(builder)
        return v5PathOp
