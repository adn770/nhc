# NHC — core dependencies
blessed
pyyaml

# Web server (optional — for nhc_web.py)
flask

# Testing
pytest
pytest-asyncio

# LLM providers (optional — install what you need)
# macOS Apple Silicon:
mlx-lm
# Ollama:
ollama
# Anthropic:
anthropic
