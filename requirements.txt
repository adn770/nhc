# NHC — core dependencies
blessed
pyyaml

# Web server (optional — for nhc_web.py)
flask
flask-sock
shapely
noise

# Testing
pytest
pytest-asyncio

# LLM providers (optional — install what you need)
# macOS Apple Silicon:
mlx-lm
# Ollama:
ollama
# Anthropic:
anthropic
